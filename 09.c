#include <stdio.h>
#include <string.h>
#include "bibliotecaVetor.h"

int main(){
	char vet[100];
	int n, i;
	printf("Quantidade de elementos do vetor:");
	scanf("%d",&n);
	
	fflush(stdin);
	gets(vet);
	mostraVetorChar(vet,n);
	
	for(i=0;i>0;i--)
	
	mostraVetorChar(vet,n);
	
	
	return 0;
}
